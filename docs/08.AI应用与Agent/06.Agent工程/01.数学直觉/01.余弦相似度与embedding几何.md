---
title: 余弦相似度与 embedding 几何
categories:
    - ai
tags:
    - ai
    - math
    - retrieval
mathjax: true
---

> 选题意图：搞清「两段文本意思接近」为什么能变成一个可以计算的数字，以及这个数字什么时候会骗人。
> 对应里程碑：M-1 闸门 1 · 直接服务于 M1 检索质量工程
> 状态：草稿

## 一句话

（不超过 30 字。不许抄定义，用你自己的话。）

## 它解决什么问题

_RAG 里"从一堆文档中找出与问题最相关的几段"这一步，本质上在做什么运算？_

## 在我代码里对应哪一行

- 文件：
- 它在做什么：

## 我的直觉解释

_提示：想象两个箭头。如果它们指向同一方向，夹角是 0；垂直就是毫无关系。点积除以两个长度，得到的就是夹角的余弦。_

- 为什么是余弦而不是欧氏距离？
- 为什么"把向量归一化"之后，余弦相似度和点积就等价了？

## 验证代码（能跑，20 行以内）

```python
# 建议做法：
# 1. 手算两个短向量的余弦相似度
# 2. 用 numpy 再算一遍对照
# 3. 造三对句子（相近 / 无关 / 相反），看分数落在哪里
```

## 这一节最容易被忽略的坑

- **维度灾难**：高维空间里"最远的点"和"最近的点"差距会变小，为什么？
- **方向 vs 幅度**：如果两段文本长度差很多，embedding 的幅度会不会影响结果？
- **不同模型不可比**：A 模型的 0.8 分和 B 模型的 0.8 分能直接比吗？

## 边界 / 反例

_什么情况下余弦相似度高，但两段文本其实不相关？_

## 待补

_没搞懂的部分写这里，然后继续往下走，不要卡住。_

---

**写完自查**：

- [ ] 能不看资料说清"为什么余弦相似度能用来做检索"
- [ ] 验证代码能跑出预期结果
- [ ] 「边界」一节写了至少一条真实的反例
